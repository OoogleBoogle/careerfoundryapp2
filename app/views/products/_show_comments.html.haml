- @comments.each do |comment|
	.row
		%hr
		%p
			= comment.user.full_name
			%br
			%small
				%em= "#{time_ago_in_words(comment.created_at)} ago"
		%p= comment.body
		%p= "Rated: #{comment.rating}/5"