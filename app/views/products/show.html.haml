.container
  .show-img= image_tag "#{@product.image_url}"
  .show-details
    %h3= @product.title
    %h4= @product.description
    %h5= "$#{@product.price}"
    = link_to "Buy now", product_orders_path(@product), method: :post

= link_to 'Edit', edit_product_path(@product)
\|
= link_to 'Back', products_path
