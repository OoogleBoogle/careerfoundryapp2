- if signed_in?
	%h4 Leave a review
	= form_for([@product, @product.comments.build]) do |f|
		= rating_for @product, nil, disable_after_rate: false, star_path: '/raty_images'
		%p
			= f.label :body, "Comments"
			%br
			= f.text_area :body
		%p
			= f.submit "Submit"